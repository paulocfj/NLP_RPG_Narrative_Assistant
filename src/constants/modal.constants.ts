/**
 * `HAS_SEEN_WELCOME_MODAL_KEY` is a constant string used as the key for
 * storing the status of the "Welcome Modal" in the browser's local storage.
 *
 * This key helps determine if a user has seen the introductory modal before,
 * preventing it from showing up automatically on subsequent visits.
 *
 * @type {string}
 */
const HAS_SEEN_WELCOME_MODAL_KEY = 'hasSeenWelcomeModal';

export { HAS_SEEN_WELCOME_MODAL_KEY };
